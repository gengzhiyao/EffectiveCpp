#include <iostream>

/**
 * !将成员变量声明为private
 * 1. 所有对类内成员的访问都是使用函数进行访问，不必考虑在使用成员时区分是成员变量和成员函数
 * 2. 用函数能够控制成员变量的访问方式：可读写，只读，只写，不可读写
 * 3. 封装性：将成员变量隐藏在函数接口背后，可以为所有可能的实现提供弹性
 *      如果不封装，任何改变 public 事物的能力会收到极端的束缚
 * Notion: public 意味着不封装 不封装代表不可改变
 * ! 成员变量的封装性与改变成员变量所破坏的代码的数量成反比
 * 一旦将成员变量声明为 public/ protected 而客户开始使用它，就很难改变客户端代码了
 */

int main ()
{

    return 0;
}